# OS Integration & Monitoring
# pygetwindow and win10toast are Windows/macOS-only; the code imports them
# behind try/except guards so the rest of Bodhi runs (and tests pass) on
# any platform.
pygetwindow==0.0.9; sys_platform == 'win32'
psutil==5.9.5
watchdog==3.0.0
win10toast==0.9; sys_platform == 'win32'

# LLM Integration (Pick whichever you prefer)
openai==1.12.0
google-generativeai==0.8.5

# Utilities
python-dotenv==1.0.0
